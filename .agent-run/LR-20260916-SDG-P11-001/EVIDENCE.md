# Evidence

Exact base0cebd8bf7ddadabbe886c68af33ca74456a0fe66 and exact packet digest bound in RUN_MANIFEST.
Baseline967 Python /159 Web PASS. Added94 Python /21 Web; current1061 /180 PASS.
Python full: python -m pytest -p no:cacheprovider --basetemp=<ignored-local> --tb=short,1061 PASS.
Existing inaccessible pytest cache bypassed without ACL changes or deletion.
Web test/lint/production build PASS. wide4/wide6/wide8/deep6 each PASS at128MiB.
Boundary/pip/diff checks PASS. Initial fixture whitelist edit missed the old FAR filename extension;
corrected to the exact three authorized Project names, then reran boundary successfully.
Two initial test expectations (legacy MassingError type and already-true review flag) corrected;
Project structural validation guard indentation repaired before the full suite.

Before code changes, exact base generated and verified synthetic Package0.1/0.2.
All five SHA-256 values per route are frozen in test_height_run; current bytes match exactly.
Three versioned create/verify CLI routes PASS. New0.3 reports5/5/0.
Effective FAR400% and height24m; heights4/5/6/7/8 produceGFA800/640/640/480/480 and floors5/4/4/3/3.
Package0.3 binds original Project in reader; height/FAR context tamper after rehash is rejected.
Unknown/absent height leaves no partial package. Zeroheight yields valid5/0/5 package.

Local production HTTP200. Playwright uses existing external runtime only, synthetic fixtures only.
Direct Search0.4/0.3/0.2/0.1, Package0.3 folder/0.2 folder/0.1 files PASS.
FAR and height base/additional/status/effective/IDs, tie IDs, review/disclaimers, Rank1..5/usage/SVG,
known0m2/0/2, Clear/all selectors,390x844/no root overflow PASS. Screenshots visually reviewed.
Initial framework GET assets distinguished from input. After selection:new requests0,POST0,input-bearing0,external0.
localStorage/sessionStorage/IndexedDB/CacheStorage/service workers0 at every checkpoint; console/runtime errors0.
Browser closed and production server stopped. Existing React checklist: pure bounded panel, escaped text,
labelled sections and caption/headings, no new effects/state duplication, network or storage APIs.
Ignored evidence includes packet, packages, logs, harnesses, screenshots, mutation copies. None committed.
Vercel NOT TOUCHED; Private Workspace NOT READ; new dependency NONE; no legal research.
Mutation/CI results will be recorded only after execution. Draft PR creation is the final action.
